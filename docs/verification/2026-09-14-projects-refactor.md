# 项目页完整重构验收

> 本记录对应首轮工程重构。当前视觉已更新，见 [视觉重构验收](2026-09-14-projects-visual-redesign.md)。

日期：2026-09-14。工作目录为 `NexoFolio-Front` 当前检出；没有创建 worktree、修改后端、提交、推送或发布。保留此前未提交工作。

## 工程检查

- `pnpm test`：3 个文件，33 项通过。其中请求层 13 项、项目查询与最近访问 11 项、准入与转场生命周期 9 项。
- `pnpm build`：Vue/TypeScript 类型检查和 Vite 生产构建通过，生产项目适配器默认使用旧接口能力。
- `git diff --check`：通过。对本轮编辑文件执行 Prettier；未批量改写无关源文件。
- 新增回归覆盖：跨全部 Mock 集合过滤后分页、旧接口禁止发送新参数、IME/250ms 防抖/Enter、120ms 进度、取消和迟到响应、失败重试、页码收缩、视图隔离与退出清理、历史上限/去重/并发/存储异常/403/404/503、重复进入、卸载取消、导航失败和过期 runId。

## 浏览器环境

项目数据使用开发 Mock。隔离 Vite 预览运行在 4189，临时本地认证响应运行在 4188，只返回虚构用户及演示会话。没有读取真实账号密码或执行后端业务写入；应用本身的登录守卫未放宽。异常列表测试仅临时替换该浏览器内存中的项目 source，检查后已恢复。

Ego 浏览器的 `Page.captureScreenshot` 超时，视觉交付改由 Codex 内置浏览器完成。截图采用普通视口捕获；全页截图在该通道存在缩放异常，未用作交付。

## 实测结果

| 检查                        | 结果                                                                                             |
| --------------------------- | ------------------------------------------------------------------------------------------------ |
| 1440×900、1280×720、390×844 | 页面外层尺寸等于视口，无整页横向或纵向溢出；64px 顶栏；分页始终可达                              |
| 行与表头                    | 桌面 56px / 44px；手机 66px 紧凑两列，不产生隐藏空列                                             |
| 滚动与返回                  | 手机 PageDown 后内部 scrollTop=432，外层=0；进入项目返回后恢复为 432；表头仍位于外框顶部         |
| 水印                        | clip 容器已消除负偏移造成的隐藏外层滚动；纯白/纯黑主题下分别使用 2.5%/4.5%                       |
| 搜索                        | 聚焦实测 560px、失焦 320px；权限按钮 x=1184 保持；输入无额外 outline；清除保留焦点               |
| 全项目查询                  | “开放服务”能搜索到原本位于第二页的项目；“平台”匹配 6 条；可访问筛选共 13 条，每页仍 10 条        |
| 零结果和失败                | 零匹配显示独立空状态并隐藏最近分组；模拟网络故障保留“平台”查询，重试恢复 6 条结果                |
| 权限菜单                    | 键盘 ArrowDown/Enter 选择成功、Escape 返回；390×360 边缘用例向上翻转，背景不透明，变换原点在底部 |
| 名称与状态                  | 长名称省略且 Tab 聚焦显示完整 Tooltip；未知状态、暂停、关闭、权限未知和拒绝有独立呈现            |
| 最近访问                    | 真实执行多个成功准入/导航后出现最多三条；全部项目分页计数独立，不因快捷分组增加                  |
| 权限拒绝                    | 原地展示说明、无成功遮罩，关闭后焦点归还对应进入按钮，滚动位置保持                               |
| 项目转场                    | DOM 起止测量 2000.2ms，包含淡出；视频包含真实流光和目标页                                        |
| 登录转场                    | 独立登录遮罩测量约 3004ms（浏览器调度误差），与项目转场独立                                      |
| 后台恢复                    | 转场中用 CDP 冻结页面 2300ms 后恢复，遮罩正确清理，无旧回调残留                                  |
| 主题与减少动态效果          | 跟随系统从深到浅实时响应；深色底 rgb(0,0,0)、表头 rgb(20,20,20)；减少动态效果下列表动画关闭      |

浏览器验收中实际发现并修复两项问题：移动端表格隐藏列仍占位，以及水印负偏移导致 overflow:hidden 的父层被焦点滚动。修复后的截图和布局读数已覆盖这两个问题。

## 截图与录屏

本地交付目录 `artifacts/projects-refactor/` 被 gitignore 排除，不会进入生产包。

- `light.png`、`dark.png`、`menu.png`、`focus.png`：1440×900 实际页面截图。
- `mobile.png`：390×844；`desktop-1280.png`：1280×720。
- `search.mp4`：6 秒，实际浏览器帧，展示聚焦扩展、输入、跨页搜索结果、清除和收回。
- `enter-project.mp4`：约 4.5 秒，实际浏览器帧，展示准入后 2 秒 Logo＋字标流光与目标页。
- `*-frames/frames.json` 保存采样时间，MP4 按实际帧间隔编码（约 12–15fps），不是用静态图伪造动效。原始帧保留供核查。
- `layout-checks.json`、`transition-timing.json`、`login-timing.json`、`background-check.json`：读数记录。
- `index.html`：截图和录屏索引。

## 明确的边界与缺口

- 200% 使用 720×450、640×360 等效 CSS 视口检查重排；尚未完成原生浏览器菜单 200% 缩放验收。390px 在 200% 下对应的 195px 视口被内置浏览器限制到 240px，因此该极窄尺寸不能视为完整验收。
- 未进行真实手机设备、真实禅道账号或真实后端新搜索接口的联调。用户隔离、存储异常和并发迟到等故障边界主要由自动化测试覆盖。
- 新查询参数必须等后端确认部署后，再显式设置 `VITE_PROJECTS_QUERY_API=true`；生产默认旧接口模式会明确禁用搜索和权限筛选。
- 目标接口文档仍是既有待设计占位，本轮没有扩大成接口文档实现。
