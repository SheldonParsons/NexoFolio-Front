{
  "$defs": {
    "CaptureReceipt": {
      "properties": {
        "ingestion_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        },
        "observation_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        },
        "reason_code": {
          "type": "string"
        },
        "record_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        },
        "record_index": {
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "replayed": {
          "type": "boolean"
        },
        "retryable": {
          "type": "boolean"
        },
        "status": {
          "$ref": "#/$defs/CaptureReceiptStatus"
        },
        "structure": {
          "type": "string"
        }
      },
      "required": [
        "record_index",
        "status",
        "reason_code",
        "retryable",
        "structure",
        "replayed"
      ],
      "type": "object"
    },
    "CaptureReceiptStatus": {
      "enum": [
        "accepted",
        "ignored",
        "rejected"
      ],
      "type": "string"
    },
    "Environment": {
      "properties": {
        "id": {
          "format": "uuid",
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "name"
      ],
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "batch_id": {
      "format": "uuid",
      "type": "string"
    },
    "environment": {
      "anyOf": [
        {
          "$ref": "#/$defs/Environment"
        },
        {
          "type": "null"
        }
      ]
    },
    "results": {
      "items": {
        "$ref": "#/$defs/CaptureReceipt"
      },
      "type": "array"
    },
    "schema_version": {
      "const": "3"
    }
  },
  "required": [
    "schema_version",
    "batch_id",
    "results"
  ],
  "title": "CaptureBatchReceipt",
  "type": "object"
}
